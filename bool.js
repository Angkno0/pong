let paused= false;
console.log(paused);

paused = !paused;
console.log(paused);

paused = !paused;
console.log(paused);



const gameArea = document.querySelector("#gameArea");
const gameAreaRect = gameArea.getBoundingClientRect();

if( paddle1Rect.bottom > gameAreaRect.bottom){

}

// ikkje del av oppgåven men har berre ikkje skiftet plass for denne filen.